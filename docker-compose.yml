services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: worldbank
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
